#include "../include/f_function.h"
#include "../include/h.h"
#include "../include/hypercube.h"
#include <iostream>
#include <vector>

std::vector<int> vector_hypercube(H h, int dk) {
    std::vector<int> p;
    for (int i; i < dk; i++) {
        // p[i]=f_func(h);
    }
}